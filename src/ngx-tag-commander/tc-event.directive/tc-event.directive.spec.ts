import { TcEventDirective } from './tc-event.directive';

describe('TcEventDirective', () => {
  it('should create an instance', () => {
    const directive = new TcEventDirective();
    expect(directive).toBeTruthy();
  });
});
